# SMTP Configuration for Email Inquiries
# These variables are required for the inquiry form to send emails

# SMTP server hostname (e.g., smtp.gmail.com, smtp.office365.com)
SMTP_HOST=

# SMTP server port (typically 587 for TLS, 465 for SSL)
SMTP_PORT=

# SMTP authentication username (usually your email address)
SMTP_USER=

# SMTP authentication password or app-specific password
SMTP_PASS=

# From email address (optional, defaults to SMTP_USER if not set)
SMTP_FROM=

# Recipient email address for inquiry submissions (defaults to batinhincer@frezya.nl)
INQUIRY_EMAIL=batinhincer@frezya.nl

# Base URL for the website (used in email links)
# For development, this can be http://localhost:3000
NEXT_PUBLIC_BASE_URL=https://virelia.com

# Note: For local development, if SMTP is not configured, 
# inquiry submissions will be logged to console instead of sending emails.

# =============================================================================
# Sanity CMS Configuration
# =============================================================================
# These variables are optional. If not set, the site will use local data from
# site/data/products.ts as a fallback.

# Sanity project ID (required for CMS features)
NEXT_PUBLIC_SANITY_PROJECT_ID=

# Sanity dataset (defaults to 'production')
NEXT_PUBLIC_SANITY_DATASET=production

# Sanity API version (defaults to '2024-01-01')
SANITY_API_VERSION=2024-01-01

# Sanity read token (optional, for private datasets only)
SANITY_READ_TOKEN=

# Sanity write token (required for saving inquiries to Sanity CMS)
# Generate a token at: https://sanity.io/manage → Your Project → API → Tokens
# Select "Editor" or "Read + Write" permissions
# If not set, inquiries will still be sent via email but not stored in Sanity
SANITY_WRITE_TOKEN=

# =============================================================================
# Site Configuration
# =============================================================================

# Production site URL (used for SEO, sitemap, and canonical URLs)
NEXT_PUBLIC_SITE_URL=https://frezya.nl

# =============================================================================
# Analytics Configuration (Phase 11)
# =============================================================================

# Google Analytics 4 Measurement ID (optional)
# Set this to enable analytics tracking in production
# Example: G-XXXXXXXXXX
# Leave empty to disable analytics
NEXT_PUBLIC_ANALYTICS_ID=

# =============================================================================
# Build Information (Phase 11)
# =============================================================================

# Git commit hash (optional, auto-set by Vercel as VERCEL_GIT_COMMIT_SHA)
# Used by /api/version endpoint
# GIT_COMMIT=

# Build timestamp (optional)
# Used by /api/version endpoint
# BUILD_TIME=
