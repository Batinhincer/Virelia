# Sanity Studio Configuration
# These variables are required for the Studio to connect to your Sanity project

# Sanity project ID (required)
SANITY_STUDIO_PROJECT_ID=

# Sanity dataset (defaults to 'production')
SANITY_STUDIO_DATASET=production

# Sanity write token (required for seeding script)
# Generate a token at: https://sanity.io/manage → Your Project → API → Tokens
# Select "Editor" or "Read + Write" permissions
SANITY_WRITE_TOKEN=
